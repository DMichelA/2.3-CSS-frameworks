<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Formulario con Bootstrap Framework</title>
    <link rel="stylesheet" href="style.css">
    <link href="{{=URL('static', 'css/web2py.css')}}" rel="stylesheet" type="text/css" />
 
</head>

<body>
  <h1>FORMULARIO BOOTSTRAP FRAMEWORK</h1>

      <form class="form-bootstrap" action="">

        <div class="field">
            <label for="matricula">Matricula:</label>
            <div class="col-sm-10">
                <input type="text" class="form-control" id="matricula" placeholder="Introduce matricula">
            </div>
        </div>
        <div class="field">
            <label for="name">Nombre:</label>
            <div class="col-sm-10">
                <input type="text" class="form-control" id="name" placeholder="Introduce nombre">
            </div>
        </div>
        <div class="field">
            <label for="onelastname">Primer Apellido:</label>
            <div class="col-sm-10">
                <input type="text" class="form-control" id="onelastname" placeholder="Introduce primer apellido">
            </div>
        </div>
        <div class="field">
            <label for="twolastname">Segundo Apellido:</label>
            <div class="col-sm-10">
                <input type="text" class="form-control" id="twolastname" placeholder="Introduce segundo apellido">
            </div>
        </div>
        <div class="field">
            <label for="edad">Edad:</label>
            <div class="col-sm-10">
                <input type="text" class="form-control" id="edad" placeholder="Introduce edad">
            </div>
        </div>
        <div class="field">
            <label for="date">Fecha de Nacimiento:</label>
                <input type="date" class="control" id="date" placeholder="Introduce fecha de nacimiento">
        </div>
        <div class="field">
            <label for="sexo">Sexo:</label>
            <select class="control" id="sexo">
              <option selected>Seleccionar...</option>
              <option>Femenino</option>
              <option>Masculino</option>
            </select>
        </div>
        <div class="field">
            <label for="inputState">Estado:</label>
            <select id="inputState" class="control">
              <option selected>Seleccionar...</option>
              <option>Soltero</option>
              <option>Casado</option>
              <option>Divorciado</option>
              <option>Otro</option>
            </select>
        </div>
                <button type="submit" class="boton">Guardar</button>
        </div>
      </form>
</body>
</html>
